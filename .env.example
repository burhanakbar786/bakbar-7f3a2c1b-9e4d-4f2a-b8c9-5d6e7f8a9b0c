# Environment Configuration

# Database
DATABASE_TYPE=sqlite
DATABASE_PATH=./apps/api/data/database.sqlite

# JWT Configuration
# ⚠️ IMPORTANT: Change this to a strong, randomly generated secret in production
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=turbovets-super-secret-jwt-key-7f3a2c1b-9e4d-4f2a-b8c9-5d6e7f8a9b0c
JWT_EXPIRATION=24h

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
# Frontend URL for CORS whitelist
FRONTEND_URL=http://localhost:4200

# Optional: PostgreSQL Configuration (for production)
# DATABASE_TYPE=postgres
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USERNAME=postgres
# DATABASE_PASSWORD=your_password
# DATABASE_NAME=turbovets_db
